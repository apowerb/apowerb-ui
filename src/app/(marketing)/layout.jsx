export const metadata = {
  title: "TH2 Agent Studio — Build AI Agents That Actually Work",
  description:
    "Create, orchestrate, and supervise autonomous AI agents — all in one platform. No code required. From idea to production in minutes.",
};

export default function MarketingLayout({ children }) {
  return <>{children}</>;
}
